@page "/{folder}"

@inject NavigationManager Navigation

<iframe src="@($"{Navigation.BaseUri}{Folder}/index.html")" class="static-page"></iframe>

@code {
    [Parameter]
    public string Folder { get; set; } = "";
}